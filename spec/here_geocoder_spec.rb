require 'spec_helper'

describe HereGeocoder do
  it 'has a version number' do
    expect(HereGeocoder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
